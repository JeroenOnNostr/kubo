# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
yarn.lock
deploy.sh

# TypeScript incremental build cache (KUBO-175)
*.tsbuildinfo

# Editor directories and files
.idea
.DS_Store
.project
.classpath
.settings/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel

# Secrets
.env
.env.*
!.env.example

# Build-time configuration
ditto.json

# Android build outputs and sensitive files
*.aab
*.p12
resources/
android/app/my-upload-key.keystore
android/app/kubo-release.p12
android/key.properties
android/local.properties

# Google Play Developer API service-account key (can publish to the WOTF Play account).
# Drop the downloaded JSON at play-store/play-service-account.json; NEVER commit it.
play-store/play-service-account.json
**/play-service-account*.json
**/kubo-play-publish-*.json
**/*.iam.gserviceaccount.com.json

# Play publish tooling (Python) bytecode cache
play-store/__pycache__/
**/__pycache__/
*.pyc
android/build/
android/.gradle/
android/*/build/
android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png
android/downloads/

# Build artifacts (signed APKs etc.) — kept locally, never committed
artifacts/
