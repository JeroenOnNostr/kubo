# Kubo — TODO

Issue prefix: `KUBO-xxx`

Completed work lives in [DONE.md](DONE.md).

## Open

- **KUBO-001: Upload flow — verify "Publish as" matrix in browser**
  Implementation landed: `getSignerForPubkey` in `src/pages/ContentUploaderPage.tsx` now rebuilds a signer from any stored login type (nsec / extension / bunker) instead of only `type === 'nsec'`. This should unblock "Publish as Parent" while a kid is the active login when the parent logged in via NIP-07 extension or NIP-46 bunker. Typecheck + lint pass; **not yet browser-verified**. Walk the matrix on `/parent/upload` with `npm run dev` (Pixel 7 emulation): (1) parent-nsec / parent-active / publish-as-parent, (2) parent-nsec / parent-active / publish-as-kid, (3) parent-nsec / kid-active / publish-as-parent, (4) parent-extension / kid-active / publish-as-parent, (5) parent-bunker / kid-active / publish-as-parent, (6) any / any / publish-as-kid. For each row confirm a kind-20/21 event is published and attributed to the right pubkey, and that no KUBO-016 render-loop fires (the fix path deliberately avoids `setLogin`). Negative case: a pubkey with no matching login record should still surface the existing `"That account's key isn't loaded on this device."` toast.

- **KUBO-011: App-wide biometric / device-PIN gate for nsec reveal and export**
  Neither Kubo nor upstream Ditto currently gates nsec reveal behind any authentication — the Eye toggle in `KidKeysPage`, `ProfileSettings.BackupKeySection`, and `InitialSyncGate` is a plain `useState`. Install a Capacitor biometric plugin (e.g. `@capacitor-community/biometric-auth`) with a web fallback (AlertDialog "type NSEC to confirm"), and wrap the reveal/export paths with a shared `<RequireAuth>` helper so the gate covers both parent and kid nsec surfaces.

- **KUBO-016: Signer-swap render-loop crash on `/parent/kid/:id/feed-settings`**
  Pre-existing bug: after `setLogin(kidLoginId)` fires in `useEditAsKid`, the target page throws *Maximum update depth exceeded* in a Radix `<Switch>` (`setRef` inside an `Array.map` in `dist-D0IwgzoO.js`) and the ErrorBoundary takes over. `/settings/network` with the same `<RelayListManager />` + `<BlossomSettings />` under a stable login renders fine, so the trigger is the account-switch cascade, not those components. Likely culprit: one of the `updateConfig`-in-effect blocks in `NostrSync.tsx` (relayMetadata sync, Blossom sync, or encrypted-settings sync — all depend on `user?.pubkey` and call `updateConfig(...)` on every run) fires repeatedly when the kid's `kind:10002`/`30078` events haven't been seeded into the query cache yet, feeding back into itself via `config.*.updatedAt` deps. Repro: `python3 /tmp/kubo-trust-places-verify.py` against `npm run dev`. Fix direction: make the NostrSync effects idempotent against "event not yet fetched" state (skip `updateConfig` if the fetched event is older than `updatedAt`, or gate on `isFetched` rather than presence of data).

- **KUBO-021: Videos tab — trust-scoped filtering**
  Follow-up to KUBO-020. `VideosTab` on `/parent/profile/:npub` currently shows every media event from the creator, unfiltered. Once KUBO-102 defines the trust → feed-routing mapping, filter `useProfileMedia` results by the current kid's trusted-authors set (or the kid's active trust level) so the grid matches what the kid is allowed to see. Probably a thin wrapper hook `useKidScopedProfileMedia(pubkey, kidId)` that post-filters the infinite query pages.

- **KUBO-103: Profile videos — "Next post" affordance like the kid feed**
  The kid feed uses `NextPostFAB` (`src/components/kid/NextPostFAB.tsx` + `KidFeedList.tsx`) to advance the kid one post at a time as a paced browsing affordance, gated by the per-kid `nextPostButton` setting in `EditKidSettingsPage`. The parent profile `/parent/profile/:npub` Videos tab is a free-scroll 3-column grid — no equivalent "show me the next set" affordance when a creator has a long tail. Add a similar control on the profile Videos tab so users can step through a creator's catalog in chunks rather than scrolling. Open questions: parent profile tab, kid-themed `KidProfileViewPage` `KidVideosTab`, or both? Does "next" advance one tile or a full grid page (~20)? Should it respect the same per-kid `nextPostButton` setting, or always-on for parents? Implementation likely shares the `useProfileMedia` infinite query landed in KUBO-022 — the FAB programmatically calls `fetchNextPage()` and scrolls to the next batch's first tile.

- **KUBO-028: Real video categorization model**
  Category chips (All / Animals / Music / Craft / Stories) were removed in KUBO-026 because NIP-71 video events rarely carry `#t` topic tags, making non-"All" selections return empty feeds. Reintroduce once there's a backing data model: our own addressable event, a NIP-50 search query per category, an ML-based classifier running on thumbnails/titles, or an adopted `#t` convention. Until then, the chips were misleading UI.

- **KUBO-036: Full upstream Ditto sync into `brand/main` (298 commits behind as of 2026-05-07)**
  `brand/main` is now **298 commits behind `upstream/main`** (last common ancestor `8b311bde`, latest upstream `4b9fe24b`, latest upstream tags v2.13.0 / v2.13.1). Notable upstream additions: full Bitcoin wallet + on-chain zap flow, NIP-46 nostrconnect UX overhaul (live progress + APP_RELAYS fallback), NSecSignerBtc PSBT signing, NIP-07 signer injection into nsites, blobbi rooms/furniture features, kind 12473 birding events, removal of direct-messaging (we keep ours via Marmot/NIP-29), and the `0d1fe7bb` LoginDialog race fix already cherry-picked as KUBO-110. **Do not start this until in-flight groups/NIP-29 work is committed and the tree is clean.** Flow per [kubo.md](../../.claude/projects/-home-jeroen-VScode-workspace-for-building-nostr-apps/memory/kubo.md): `git checkout main && git fetch upstream && git merge --ff-only upstream/main && git push origin main && git checkout brand/main && git merge main`, resolve conflicts in favor of Kubo branding, push. Expect conflict zones: routing, layout, anywhere Kubo has rebranded, NIP-46 paths (KUBO-110 cherry-pick will collapse cleanly), `package-lock.json` for the `@nostrify/*` minor bump (0.51→0.52, 0.5→0.6, 0.36→0.37). Worth drafting a separate plan when ready — the merge is large enough that it's a session of its own.

- **KUBO-037: Set up Android release-signing keystore**
  v0.1.0 and v0.1.1 APKs are debug-signed (shared dev key across all developers' machines) — fine for personal testing but blocks Play Store distribution, means users can't cleanly update to a release-signed build later, and gives no signing-identity guarantee. Run `npm run keygen` to generate an upload keystore, fill in `android/key.properties` with the alias/passwords, and the existing `signingConfigs.release` block in `android/app/build.gradle` will pick it up. Store the keystore + passwords somewhere durable (losing them means losing the ability to update the app). Only unblock once we're ready to distribute beyond Jeroen's own device.

- **KUBO-057: Select communities page — improve search reliability**
  On `/parent/feed/communities` (`CommunitiesSourcePage.tsx`) the search pill returns inconsistent results for kind-34550 community queries. Audit the NIP-50 query, relay set, and result dedupe.

- **KUBO-063: Trust → People — wire up Groups section to real group functionality**
  `TrustPeoplePage` renders a hardcoded `GROUPS` array with two placeholder entries ("Soccer team B3", "Elementary class 4B") that link to placeholder routes under `/parent/groups/:id`. Use case: parents form a group with other parents, or set up a group around the kid's context (sports team, school class) so members can DM each other or share media scoped to the group. Decide the data model first — likely NIP-29 relay-based groups or NIP-51 kind:30000 follow sets — then wire create/join, member list, and group detail page. Likely worth splitting into separate issues once scoped: (a) group data model + create/join flow, (b) group detail screen replacing `/parent/groups/:id` placeholder, (c) group DM thread, (d) group media upload/share. Capture sub-issues as KUBO-063a/b/c/d when ready.

- **KUBO-102: Wire trust assignments to actual feed routing** *(replaces the now-removed KUBO-013)*
  KUBO-100 introduced `relayTrustAssignments[kid][relayUrl]` and KUBO-097/098/100 cleaned up `trustAssignments[kid][pubkey]`, but both maps are visual-only — they don't yet influence which content the kid sees. Decide the routing model: should `extend` mean "merge this relay's contents into the kid's feed", `interact` allow replies/zaps, `view` gate appearance in the People tab? And do we mirror that to NIP-65 `r`-tags + a kid-scoped `kind:30078` for relays so the assignments survive cross-device login? Once the model is fixed, wire `useFeedSources` / `useKidFeedRelays` to read from these maps instead of the existing per-source toggles, and define what happens to a relay or pubkey that's enabled in a feed-source page but unassigned in trust (and vice versa).

- **KUBO-109: Reply rendering + composing in NIP-29 group chat**
  In Trust-domain group chats (e.g. "Kubo testers" on `/parent/groups/:id`), replies sent from another client (0xchat, etc.) render as plain bubbles with no indication of which message they're answering, and Kubo's input bar has no way to start a reply. Both sides are now wired: `getReplyTarget` in `src/lib/nip29.ts` parses `e`-tag markers (`reply` / `root` / unmarked NIP-10 / `q` fallback) to find the parent; `MessageBubble` exposes a `ReplyPreviewStrip` that renders a tappable "replying to <author>" strip inside the bubble (themed against orange-on-primary for outgoing, muted-on-card for incoming) plus an italic "Replied message" placeholder while the parent is loading via `useEvent`; tapping the strip scrolls and briefly highlights the parent row using `data-message-id` + `data-highlight`. Compose: long-press (mobile, 500ms with `navigator.vibrate`) or right-click (desktop) any non-pending bubble to set it as the reply target; a chip above the input shows the parent's author/excerpt with an X to cancel; `Esc` clears it; submit calls the existing `sendMessage(content, replyTo)`. Verify in browser per plan steps 2–9.

- **KUBO-110: Verify upstream remote-signer fix on Android with Amber (bundled into next Zapstore release)**
  Cherry-picked `0d1fe7bb` from upstream Ditto onto `brand/main` (commit `e929eb15`) — fixes a race in `src/components/auth/LoginDialog.tsx` where the nostrconnect listening effect re-ran on every parent render, flipped a local `cancelled` flag, and silently skipped `onLogin()`/`onClose()` when the signer's NIP-46 response finally arrived (user was logged in on the backend but the dialog never closed). Surfaced upstream by an Amber listening-REQ delay; matches the symptom Jeroen reported on the Kubo Android build with Amber. Kubo had not modified `LoginDialog.tsx` since divergence so the cherry-pick applied cleanly. **Verification path:** no local Android emulator available, so this rides the next Zapstore release (tonight). After downloading the new APK on phone, log in via Amber → tap "Open Signer App" → authorize → confirm Kubo dialog closes without re-tap. If the bug persists, revert with `git revert e929eb15` and investigate Kubo-specific layers (`LoginDialog` wrappers, `NostrProvider` re-renders, Capacitor bridge timing). Once confirmed on the released build, move to DONE.md.

- **KUBO-105: App-wide copy pass — replace jargon with layman-friendly wording**
  Kubo is aimed at parents and kids, not Nostr power users, but the UI is still littered with protocol vocabulary that means nothing to a layman. Example: the word "relays" appears throughout settings, trust, and feed-source screens — most non-technical users have no mental model for a relay, so swap it for "sites", "servers", "sources", or whatever fits the surface (e.g. "Add a site" instead of "Add a relay", "Servers your kid follows" instead of "Relays"). Do a full sweep across the app for similar jargon: "nsec/npub" → "secret key / public key" or "account key", "pubkey" → "account", "kind" / event-kind numbers → drop or rename, "zap" → "tip" or "send sats", "NIP-XX" → remove from user-facing strings, "follow set" / "follow list" wording, "blossom" → "media host" or similar, "DM" already fine but check "encrypted DM" phrasing, etc. Audit `src/pages/`, `src/components/`, and any i18n/string files; produce a single mapping table first (jargon → plain term per context) before mass-editing so terminology stays consistent. Keep developer-facing strings (debug screens, error logs) untouched. Inspiration: https://paulgraham.com/simply.html 

## Deferred to post-MVP

- **KUBO-002: Separate devices** — parent and kid on distinct devices rather than sharing one; requires some transport between them (pairing, key sync, etc.).
- **KUBO-003: Parent keypair usage** — first-class parent identity with its own nsec (not just a signer-for-kid). Needed for guardian-list events, co-signing, parent-to-parent trust.
- **KUBO-004: Guardian list kind** — custom Nostr kind listing authorized guardians for a kid account. Schema design + NIP-style doc.
- **KUBO-005: Co-signing / recovery** — multi-guardian threshold signing on sensitive actions (key rotation, account recovery, trust-domain edits).
- **KUBO-006: QR handoff** — secure pairing between parent and kid device via QR (initial setup, device swap, guardian addition).
